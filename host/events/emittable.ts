export enum global {
  config = "config"
}

export enum controller {
  lastData = "last data",
  clientError = "client error"
}

export enum scoreboard {
  update = "update",
  notReady = "not ready"
}
